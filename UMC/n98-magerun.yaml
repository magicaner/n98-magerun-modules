autoloaders:
  # Namespace => path to your libs
  UMC: %module%/src/

  # or in project based config i.e.:
  # MyCommandNamespace: %root%/lib

commands:
  customCommands:
    - UMC\EnableCommand
    - UMC\DisableCommand
    - UMC\RemoveCommand
